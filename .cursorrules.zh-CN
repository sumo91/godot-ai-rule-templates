# Cursor Rules For Godot Projects

- 对于 GDScript、JSON、文档、配置文件，以及可安全文本编辑的 `.tscn` / `.tres` 变更，优先直接编辑文本。
- 如果任务完全是文本型工作，而且易于通过 diff 审查，就不要启动 Godot。
- 只有当具体工具已被证明可靠时，才使用 MCP 风格的 Godot 工具来进行编辑器启动、项目检查、调试输出和自动化。
- 对于 inspector 修改、拖拽式连线、动画设置、Tile 编辑、导入流程、布局工作和视觉验证，使用标准 Godot 编辑器。
- 对于 CLI 诊断、脚本执行、验证、CI 以及终端可见日志，使用 Godot 控制台可执行文件。
- 在可行时尽量使用带类型的 GDScript。
- 保持函数聚焦、脚本职责单一。
- 默认优先组合；只有当它明显符合 Godot 场景或类模型时才使用继承。
- 优先使用 `@onready`、导出引用或显式依赖设置，而不是脆弱的深层节点查找。
- 使用清晰、最小化的信号来实现解耦的事件式通信。
- 对始终需要的资源使用 `preload()`，对动态资源使用 `load()`。
- 对缺失资源和无效内容数据提供明确错误或安全回退。
- 保持命名一致：
  - 文件、变量、函数：`snake_case`
  - 类：`PascalCase`
  - 常量：`ALL_CAPS_SNAKE_CASE`
  - 信号：`snake_case`
- 当工作开始变得更依赖编辑器或视觉判断时，在一个干净检查点停下，并记录已准备的文件、所需编辑器操作、成功标准，以及下一步验证内容。
